{{!< default}}
{{> header}}
<main class="main_unvx pure-g" role="main">
  <div class="pure-u-1">
    <h1 class="title">{{t "Articles tagged with:"}} {{tag.name}}</h1>
  </div>
  {{#foreach posts limit=12}}
    {{> article-item}}
  {{/foreach}}
</main>
{{> footer}}